language: python
python:
  - "2.7"

branch:
    only:
        - master
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: nosetests

notifications:
    email:
      recipients:
        - abc549825@163.com
        - xiechao06@gmail.com
